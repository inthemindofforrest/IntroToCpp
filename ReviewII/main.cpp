#include "Header.h"


int main()
{
	ADDRESSBOOK AddressBook;


	//char CharString1[] = "Timmy is ";
	//char CharString2[] = "Write a function that reformats or rewraps a string to text to be no more than a given number of "
	//	"characters per line. If the text is in the middle of a word at the limit, the entire word is dropped onto the next line.";
	//char * FinalCharString = new char[strlen(CharString1) + strlen(CharString2)]();

	//InsertIntoString(CharString1, CharString2, FinalCharString,3);
	//cout << FinalCharString << endl;
	//CharacterLimit(CharString2, 20);
	//cout << CharString2 << endl;

	AddressBookCommands(AddressBook);

	system("PAUSE");
	return 0;
}